class Solution {
    public int strStr(String haystack, String needle) {
        
        if(needle.isEmpty()){
            return 0;
        }
        else{
            int position = haystack.indexOf(needle);
            return position;
        }
        
    }
}
